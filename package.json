{
  "name": "diariamente-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper iOS nativo (Capacitor) do app Diariamente",
  "scripts": {
    "build": "echo 'O conteudo e servido ao vivo de app.diariamente.club (server.url no capacitor.config.json)'"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
