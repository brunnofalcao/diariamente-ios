workflows:
  ios-diariamente:
    name: Diariamente iOS (Capacitor)
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: AppStoreConnectKey
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: club.diariamente.app
      vars:
        BUNDLE_ID: "club.diariamente.app"
        XCODE_SCHEME: "App"
      node: 20
      xcode: latest
      cocoapods: default
    scripts:
      - name: Instalar dependencias
        script: npm install
      - name: Gerar projeto iOS
        script: |
          npx cap add ios || true
          npx cap sync ios
      - name: Instalar Pods
        script: |
          cd ios/App && pod install
      - name: Aplicar assinatura no projeto
        script: |
          xcode-project use-profiles
      - name: Build do ipa
        script: |
          xcode-project build-ipa --workspace "ios/App/App.xcworkspace" --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
